<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sidebar Circular</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            overflow-x: hidden;
        }

        .sidebar {
            position: fixed;
            left: 0;
            top: 0;
            width: 80px;
            height: 100vh;
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(20px);
            border-right: 1px solid rgba(255, 255, 255, 0.2);
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px 0;
            transition: width 0.3s ease, background 0.3s ease;
            z-index: 1000;
            box-shadow: 5px 0 25px rgba(0, 0, 0, 0.1);
        }

        .sidebar.expanded {
            width: 250px;
            background: rgba(255, 255, 255, 0.25);
        }

        .sidebar-btn {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            border: none;
            background: linear-gradient(135deg, #ff6b6b, #ee5a24);
            color: white;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 10px 0;
            font-size: 18px;
            font-weight: bold;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            position: relative;
            overflow: hidden;
        }

        .sidebar-btn:hover {
            transform: scale(1.1);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
        }

        .sidebar-btn:active {
            transform: scale(0.95);
        }

        .toggle-btn {
            background: linear-gradient(135deg, #4ecdc4, #44a08d);
            margin-bottom: 30px;
        }

        .menu-btn {
            background: linear-gradient(135deg, #667eea, #764ba2);
        }

        .bottom-btn {
            background: linear-gradient(135deg, #f093fb, #f5576c);
            margin-top: auto;
            margin-bottom: 20px;
        }

        .btn-text {
            position: absolute;
            left: 70px;
            white-space: nowrap;
            opacity: 0;
            pointer-events: none;
            transition: opacity 0.3s ease;
            color: white;
            font-weight: 500;
            font-size: 14px;
            background: rgba(0, 0, 0, 0.7);
            padding: 8px 12px;
            border-radius: 20px;
            backdrop-filter: blur(10px);
        }

        .sidebar.expanded .btn-text {
            opacity: 1;
            pointer-events: all;
        }

        .main-content {
            margin-left: 80px;
            padding: 40px;
            transition: margin-left 0.3s ease;
            min-height: 100vh;
        }

        .sidebar.expanded + .main-content {
            margin-left: 250px;
        }

        .content-card {
            background: rgba(255, 255, 255, 0.9);
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(10px);
        }

        .content-card h1 {
            color: #333;
            margin-bottom: 20px;
            font-size: 2.5em;
            background: linear-gradient(135deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .content-card p {
            color: #666;
            line-height: 1.6;
            font-size: 1.1em;
        }

        .ripple {
            position: absolute;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.4);
            transform: scale(0);
            animation: ripple-animation 0.6s ease-out;
            pointer-events: none;
        }

        @keyframes ripple-animation {
            to {
                transform: scale(4);
                opacity: 0;
            }
        }

        .sidebar-btn::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transition: all 0.3s ease;
            transform: translate(-50%, -50%);
        }

        .sidebar-btn:hover::before {
            width: 100%;
            height: 100%;
        }

        /* Ícones usando CSS */
        .toggle-btn::after {
            content: '☰';
            font-size: 20px;
            z-index: 1;
        }

        .sidebar.expanded .toggle-btn::after {
            content: '✕';
        }

        /* Responsive */
        @media (max-width: 768px) {
            .sidebar {
                width: 60px;
            }
            
            .sidebar.expanded {
                width: 200px;
            }
            
            .main-content {
                margin-left: 60px;
            }
            
            .sidebar.expanded + .main-content {
                margin-left: 200px;
            }
        }
    </style>
</head>
<body>
    <div class="sidebar" id="sidebar">
        <!-- Botão de toggle (círculo do topo) -->
        <button class="sidebar-btn toggle-btn" onclick="toggleSidebar()">
            <span class="btn-text">Menu</span>
        </button>

        <!-- Botões do meio -->
        <button class="sidebar-btn menu-btn" onclick="handleClick('home')">
            🏠
            <span class="btn-text">Início</span>
        </button>

        <button class="sidebar-btn menu-btn" onclick="handleClick('search')">
            🔍
            <span class="btn-text">Pesquisar</span>
        </button>

        <button class="sidebar-btn menu-btn" onclick="handleClick('favorites')">
            ⭐
            <span class="btn-text">Favoritos</span>
        </button>

        <button class="sidebar-btn menu-btn" onclick="handleClick('settings')">
            ⚙️
            <span class="btn-text">Configurações</span>
        </button>

        <!-- Botão de baixo -->
        <button class="sidebar-btn bottom-btn" onclick="handleClick('profile')">
            👤
            <span class="btn-text">Perfil</span>
        </button>
    </div>

    <div class="main-content">
        <div class="content-card">
            <h1>Sidebar Circular</h1>
            <p>Esta é uma sidebar moderna com botões circulares baseada no seu desenho. O botão do topo expande/contrai a sidebar, os botões do meio são para navegação, e o botão de baixo é para o perfil do usuário.</p>
            <br>
            <p>Clique no botão do menu (☰) para expandir a sidebar e ver os rótulos dos botões. A interface é responsiva e inclui efeitos visuais modernos como glassmorphism e animações suaves.</p>
        </div>
    </div>

    <script>
        let sidebarExpanded = false;

        function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            sidebarExpanded = !sidebarExpanded;
            
            if (sidebarExpanded) {
                sidebar.classList.add('expanded');
            } else {
                sidebar.classList.remove('expanded');
            }

            // Efeito ripple
            createRipple(event);
        }

        function handleClick(action) {
            console.log(`Ação clicada: ${action}`);
            
            // Aqui você pode adicionar a lógica específica para cada botão
            switch(action) {
                case 'home':
                    alert('Navegando para Início');
                    break;
                case 'search':
                    alert('Abrindo Pesquisa');
                    break;
                case 'favorites':
                    alert('Abrindo Favoritos');
                    break;
                case 'settings':
                    alert('Abrindo Configurações');
                    break;
                case 'profile':
                    alert('Abrindo Perfil');
                    break;
            }

            // Efeito ripple
            createRipple(event);
        }

        function createRipple(event) {
            const button = event.currentTarget;
            const rect = button.getBoundingClientRect();
            const size = Math.max(rect.width, rect.height);
            const x = event.clientX - rect.left - size / 2;
            const y = event.clientY - rect.top - size / 2;
            
            const ripple = document.createElement('span');
            ripple.className = 'ripple';
            ripple.style.width = ripple.style.height = size + 'px';
            ripple.style.left = x + 'px';
            ripple.style.top = y + 'px';
            
            button.appendChild(ripple);
            
            setTimeout(() => {
                ripple.remove();
            }, 600);
        }

        // Fechar sidebar ao clicar fora (em dispositivos móveis)
        document.addEventListener('click', function(event) {
            const sidebar = document.getElementById('sidebar');
            const isClickInsideSidebar = sidebar.contains(event.target);
            
            if (!isClickInsideSidebar && sidebarExpanded && window.innerWidth <= 768) {
                toggleSidebar();
            }
        });

        // Atalho de teclado para toggle
        document.addEventListener('keydown', function(event) {
            if (event.key === 'Escape' && sidebarExpanded) {
                toggleSidebar();
            }
        });
    </script>
</body>
</html>